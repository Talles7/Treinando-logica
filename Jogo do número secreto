# Resutado do mini curso em pró da prática
# Conclusão - Boas vindas ao jogo do número secreto

Nesse meio tempo estive desenvolvimeto deste jogo e praticando com as ferramentes nele usadas, futuramente ireri republicar de maneira mais organizada as minhas evoluções. Mas por hora veja como ficou este projeto e relembre como foi finalizar seu primeiro projeto.

```
alert('Boas vindas ao jogo do número secreto');
let maximoNumeroSecreto = prompt('Defina qual vai ser o numero maximo:');
let numeroSecreto = parseInt(Math.random() * maximoNumeroSecreto + 1);
console.log(numeroSecreto);
let chute;
let tentativas = 1;

// enquanto chute não for igual ao n.s.
while (chute != numeroSecreto) {
    chute = prompt('Escolha um número entre 1 e '+maximoNumeroSecreto);
    // se chute for igual ao número secreto
    if (chute == numeroSecreto) {
        break;
    } else {
        if (chute > numeroSecreto) {
            alert(`O número secreto é menor que ${chute}`);
        } else {
            alert(`O número secreto é maior que ${chute}`);
        }
        // tentativas = tentativas + 1;
        tentativas++;
    }
}

let palavraTentativa = tentativas > 1 ? 'tentativas' : 'tentativa';
alert(`Isso ai! Você descobriu o número secreto ${numeroSecreto} com ${tentativas} ${palavraTentativa}.`);
```
